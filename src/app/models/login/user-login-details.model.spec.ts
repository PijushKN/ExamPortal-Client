import { UserLoginDetails } from './user-login-details.model';

describe('UserLoginDetails', () => {
  it('should create an instance', () => {
    expect(new UserLoginDetails()).toBeTruthy();
  });
});
